---
inclusion: fileMatch
fileMatchPattern: ['**/*.ts', '**/*.tsx']
---

# TypeScript

See `docs/standards/Coding.md`.

- Strict mode — no `any`, no unchecked suppressions.
- Branded ids, `Result<T,E>`, explicit public return types.
- Domain terms from Master §4 — not generic `Item`, `Row`, `Data`.
