---
inclusion: fileMatch
fileMatchPattern: ['infrastructure/ai/**/*']
---

# AI Infrastructure

Business rules: `docs/MASTER_SPECIFICATION.md` §8.

- Implement `ICategorizationService` — vendor SDKs stay in `infrastructure/ai/providers/`.
- Zod-parse all LLM JSON before persist.
- Log model, prompt version, correlationId — not full BOQ bodies.
- Never write production from AI layer.
