/** Matches `Families.Name` varchar(100) — Master design §6. */
export const FAMILY_NAME_MAX_LENGTH = 100;

/** Practical UI cap for `Families.ReferenceCode` text column — approved design §6. */
export const FAMILY_REFERENCE_CODE_MAX_LENGTH = 500;

/** Practical UI cap for `Families.Description` text column — approved design §6. */
export const FAMILY_DESCRIPTION_MAX_LENGTH = 4000;

/** Matches `FamilyLevelTypes.Name` varchar(50). */
export const FAMILY_LEVEL_TYPE_NAME_MAX_LENGTH = 50;
