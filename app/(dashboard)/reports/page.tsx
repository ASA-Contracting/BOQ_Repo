import {
  ShellContent,
  ShellPlaceholder,
} from "@/components/shared/AppShell";

export default function ReportsPage() {
  return (
    <ShellContent>
      <ShellPlaceholder
        title="Reports"
        description="Read-only reporting workspace — business logic not implemented."
      />
    </ShellContent>
  );
}
